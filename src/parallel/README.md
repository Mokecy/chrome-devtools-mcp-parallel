# src/parallel

Parallel multi-instance MCP server code. See `specs/001-parallel-instances/plan.md`
and `tasks.md` for the implementation roadmap.

This directory is strictly additive to the upstream `chrome-devtools-mcp`
codebase; no upstream file is modified.
